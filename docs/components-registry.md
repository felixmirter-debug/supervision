# Components Registry

## ThemeToggle

- Path: `frontend/components/theme-toggle.tsx`
- Purpose: Compact accent-theme selector for the product shell and standalone auth screens.
- Variants: blue, red, yellow.
- Notes: Persists the selected theme in `localStorage` and applies it through the root `data-accent-theme` attribute.
